meta {
  name: admin portal login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/auth/login/?userportal=admin
  body: json
  auth: inherit
}

params:query {
  userportal: admin
}

body:json {
  {
      "username": "admin",
      "password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
