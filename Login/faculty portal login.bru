meta {
  name: faculty portal login
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/login/?userportal=faculty
  body: json
  auth: inherit
}

params:query {
  userportal: faculty
}

body:json {
  {
      "username":"faculty",
      "password":"123456"
  }
}

settings {
  encodeUrl: true
}
