meta {
  name: TEst
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/auth/login/?userportal=agent
  body: json
  auth: inherit
}

params:query {
  userportal: agent
}

body:json {
  {
      "username":"agent",
      "password":"123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
