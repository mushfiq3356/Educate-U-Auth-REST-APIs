meta {
  name: agent portal login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login/?userportal=agent
  body: json
  auth: inherit
}

params:query {
  userportal: agent
}

body:json {
  {
      "username":"bm1",
      "password":"123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
