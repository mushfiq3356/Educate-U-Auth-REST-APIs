meta {
  name: agent portal login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login/?userportal=agent
  body: json
  auth: none
}

params:query {
  userportal: agent
}

body:json {
  {
      "username":"agent",
      "password":"123456"
  }
}
